# HostelHost-Server

Backend server for the HostelHost Hostel Management ERP.

## Getting Started

1. Install dependencies: `npm install`
2. Set up `.env` (see `.env.example`)
3. Start MySQL via Docker: `docker-compose up -d` (from root)
4. Start server: `npm start`

Server runs on port 3000 by default.
